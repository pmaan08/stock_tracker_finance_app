<h1>Finance Tracker - Track Stocks</h1>

<% if (Time.now.hour>9 && Time.now.min>14) && (Time.now.hour<3 && Time.now.min<30 )   %>
  <div class="alert alert-primary" role="alert">
    <h4 class="alert-heading">Market Open!</h4>
    <hr>
    <p class="mb-0">Market will close at 3:30 pm.</p>
  </div>
<% else  %>
  <div class="alert alert-danger" role="alert">
    <h4 class="alert-heading">Market Close!</h4>
    <hr>
    <p class="mb-0">Market will open at 9:15 am.</p>
  </div>

<% end %>